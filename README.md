# teevo-9-dev (developer integration)

Repository: [kalravparsana/teevo-9-dev](https://github.com/kalravparsana/teevo-9-dev) (`main`)

| Path | Purpose |
|------|---------|
| `Frontend/` | R1 application (release **1.0.3**) |
| `launchpad-frontend/` | Launchpad UI submodule (`YorkIE-Launchpad/teevo-9`) |
| `backend/plan/` | Backend integration plans |

## Release lock

- **Frontend version:** R1  
- **Release name:** 1.0.3  

Initialize submodule when credentials are available:

```bash
git submodule update --init --recursive
```
